# In-class exercise 5: Help parallelize matrix-vector multiplication
This project implements a Dense Matrix Vector (DMV) multiplication using SIMD (Single Instruction, Multiple Data) intrinsics for performance optimization. The implementation utilizes AVX (Advanced Vector Extensions) to perform parallel computations on 8 floating-point numbers simultaneously.
